// pop: 배열의 마지막에 있는 원수를 하나 빼줌

const numbers = [10, 20, 30, 40];

const value = numbers.pop();

console.log(value);
console.log(numbers);

// push: 배열의 마지막에 원소를 추가함

numbers.push(5);
console.log(numbers);